name: Greenfield Manabrew qualification rerun

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  completed-evidence-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Historical build/test qualification completed in workflow run 33077688159. This workflow is intentionally no longer push-triggered.'
