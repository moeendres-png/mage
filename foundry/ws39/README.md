# WS-39 XMage Commander History State Restore

This branch adds a narrow Rules-Core state-load snapshot/restore API for `CommanderPlaysCountWatcher` and native regression tests. It does not synthesize historical cast/play events and does not change normal runtime event processing.
