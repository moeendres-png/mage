# WS11 actual-card semantic gate

`Q6_ACTUAL_CARD_BEHAVIOR = FAIL`.

All exact 1,678 known WS02 Oracle identities are classified. The successor mapper proves their binding to 1,677 immutable Forge semantic path signatures at the pinned Forge source. It does not treat loading, construction, global decision/hidden/replay PASS, or absence of suspicious source markers as behavior evidence.

The result is 1,678 `PARTIAL`, zero `UNKNOWN`, and zero `UNSUPPORTED`. The exact remaining frontier is 1,677 path signatures without an executable, state-asserting semantic witness. There are zero inherited global-PASS rows and zero card-name production hacks.

The CI artifact contains the complete machine-readable `PER_IDENTITY.semantic.jsonl`; each row contains Oracle identity and provenance, Forge source/hash binding, behavior signature IDs, production reachability, all required flags and evidence classes, scenario IDs, trace hashes, run/job/artifact reference slots, classification, and failure reason.

No semantic witness was asserted, so no rules divergence was adjudicated. Future witnesses must use the current official [Magic Comprehensive Rules](https://magic.wizards.com/en/rules) and [Commander rules](https://mtgcommander.net/index.php/rules/) as authority.
