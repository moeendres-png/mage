# Rules Matrix A–T — Current Qualification State

The complete A–T structure is materialized in `RULES_MATRIX_A_T.json` with
exactly 20 definitions. Each row is currently `NOT_RUN` and
`production_qualified=false`.

Previously passing targeted rules tests remain provenance inputs only. They do
not establish the missing production-boundary combination of complete typed
decisions, principal-scoped observations, named RNG/event tapes, replay, and
actual-card behavior.

```text
RULES_MATRIX_A_T_COMPLETE = FALSE
```
