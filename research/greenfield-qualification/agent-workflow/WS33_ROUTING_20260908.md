# WS33 — Live AI Routing Snapshot — 2026-09-08

This file is a routing snapshot derived from live GitHub state. It is not itself qualification evidence and must not replace workstream checkpoints or immutable artifacts.

## Live canonical state inspected

Repository:

`moeendres-png/mage`

Active canonical WS33 branch:

`work/ws33-g3-final-closure-20260902`

Live branch identity at routing inspection:

```text
HEAD = 6da237b704ba5e66c58c2334f47e36ef68ca980d
TREE = 8f6d0151e4a9095c95925d6f6065214d382bf1dc
FORGE_PIN = 8c7e9afb8e6caee88644b94e25da5852e36f8928
```

The latest canonical checkpoint persists the AF8 terminal failure and does not authorize coverage promotion.

Current retained canonical coverage before any AF8 successor promotion:

```text
TOTAL = 4188
PASS = 488
UNKNOWN = 3700
FAIL = 0
UNSUPPORTED = 0

A_UNKNOWN = 57
B_UNKNOWN = 675
C_UNKNOWN = 700
D_UNKNOWN = 920
E_UNKNOWN = 1029
F_UNKNOWN = 319
G_UNKNOWN = 0
H_UNKNOWN = 0
```

Therefore the earlier post-Generation-2 August snapshot (`259 PASS / 3929 UNKNOWN`) is historical only. G and H should not be rerun merely because older handoffs list them as early priorities.

## Current AF8 terminal evidence

Latest verified failing AF8 qualification:

```text
RUN = 34165574477
JOB = 101875828886
SOURCE_HEAD = 6120ca3ee9754cbad4600ee45008ee2455b95465
SOURCE_TREE = 264e881c43c643363f0a499764c0a81ae8285c4e
ARTIFACT = 10034108017
ARTIFACT_NAME = ws33-abc-a-rest-svar-af8-34165574477
ARTIFACT_DIGEST = sha256:5e300282cbfab475d4291b072b1c12c19ac29b64843e93b4d5cc8676b36bcf49
```

Relevant step results:

```text
Record = SUCCESS
Replay = SUCCESS
Fail-closed adjudication = FAILURE
Always-run evidence seal = SUCCESS
Artifact upload = SUCCESS
```

Persisted failure classification:

`QUALIFICATION_EVIDENCE_CONTRACT_MISMATCH`

Exact blocking symptom:

`DECISION_ID 'a-rest-svar-af8-alternatives-1-remote' missing path_id`

This evidence does **not** establish:

- a Forge semantic defect;
- a Magic rules defect;
- `CARD_BEHAVIOR_FAILURE`;
- a generic `P3` engine gap;
- a `P0` path-model defect;
- a `P5` architecture boundary.

The correct next action is source-level classification of the remote-client decision's semantic role, preserving strict path attribution for actual gameplay/discretionary decisions.

## Parallel branch state

The following isolated branches were verified present at the frozen AF8 integration source:

```text
work/ws33-a-trigger-closure-20260907
work/ws33-b-high-throughput-20260907
work/ws33-c-high-throughput-20260907
work/ws33-d-high-throughput-20260907
work/ws33-e-high-throughput-20260907
work/ws33-f-high-throughput-20260907

HEAD = 895240f4058076764227a418ad28e84f61d3a7ed
TREE = cec73ae51b0168280ea648892f3e9edd46dcd883
```

These are isolated materialization/qualification work branches, not authorization to mutate canonical coverage independently.

## Model routing

### Priority 0 — AF8 evidence-contract repair

```text
ROUTE = NORMAL_SOL_ANALYSIS -> TERRA_IMPLEMENTATION -> NORMAL_SOL_REVIEW
```

Reason:

- exact failure class is already bounded to the qualification/adjudication boundary;
- Record and Replay completed;
- no engine/rules failure is established;
- the remaining semantic question is whether the null-path remote decision is auxiliary client evidence or a path-bound gameplay decision.

Do **not** use Codex Sol yet. Current evidence does not justify P0/P3/P5 escalation.

### A-trigger materialization

Branch:

`work/ws33-a-trigger-closure-20260907`

```text
ROUTE = TERRA
```

Goal: deterministic current-UNKNOWN trigger manifest/shard infrastructure and focused witness work under the existing exact source/contracts. No coverage promotion until immutable cross-certification.

### B high-throughput

Branch:

`work/ws33-b-high-throughput-20260907`

```text
ROUTE = TERRA
```

For ordinary amount/cost materialization and witness infrastructure, Terra is the default. Any actual Cost/Payment externalization defect that changes authoritative decision semantics requires a Sol planning/review pass before promotion.

### C high-throughput

Branch:

`work/ws33-c-high-throughput-20260907`

```text
ROUTE = TERRA
```

Parent -> SubAbility/API production-route witnesses are bounded implementation/campaign work unless evidence reveals a model or core engine defect.

### D high-throughput

Branch:

`work/ws33-d-high-throughput-20260907`

```text
ROUTE = TERRA
```

Prefer state-only/common action families before decision-heavy tails.

### E high-throughput

Branch:

`work/ws33-e-high-throughput-20260907`

```text
ROUTE = TERRA for simple zone/trigger/token/destroy families
ROUTE = NORMAL_SOL -> TERRA -> NORMAL_SOL for Replacement/LKI hard tail
```

Do not infer a replacement-engine fix from remaining UNKNOWN coverage.

### F high-throughput

Branch:

`work/ws33-f-high-throughput-20260907`

```text
ROUTE = TERRA for simple pump/static families
ROUTE = NORMAL_SOL -> TERRA -> NORMAL_SOL for layers/copy/control hard tail
```

Historical WS29 semantics are not sufficient to inherit PASS under the current model.

### G / H

```text
G_UNKNOWN = 0
H_UNKNOWN = 0
```

No new routine G/H qualification should be scheduled unless a later contract/code change invalidates the existing evidence.

## Luna queue

Use Luna only after the corresponding Terra/Sol semantic work is fixed, for example:

- handoff normalization;
- documentation;
- repetitive shard formatting;
- already-specified regression fixture expansion;
- lint/compiler cleanup without semantic change;
- evidence table transformation.

Do not use Luna to decide whether null-path gameplay decisions are legal evidence, to reinterpret failure categories, or to adjudicate replacement/layer semantics.

## Codex Sol escalation gate

Current status:

```text
SOL_CODEX_REQUIRED = FALSE
```

Escalate only if later evidence establishes one of:

```text
P0 model/core representation defect
P3 generic engine Rules-Core defect
P5 architecture boundary
persistent nondeterminism/replay/concurrency root cause
two genuine Terra repair failures against the same confirmed hard root cause
```

## Recommended execution order

```text
1. AF8 evidence-contract source adjudication and bounded repair
2. Terra A-trigger materialization
3. Terra B/C/D high-throughput materialization in parallel isolated branches
4. Terra simple E/F materialization
5. Sol-assisted P4 E/F hard tails
6. serial immutable cross-certification and coverage promotion
7. WS33 final cross-qualification
8. WS01-WS33 reconciliation
9. evidence-based Architecture Freeze adjudication
```

The active canonical workstream checkpoint remains authoritative if it advances beyond this snapshot.