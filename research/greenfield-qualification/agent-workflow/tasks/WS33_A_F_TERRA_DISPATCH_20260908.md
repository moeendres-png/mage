# WS33 — A–F Terra Dispatch Packets — 2026-09-08

These packets are execution prompts for the already-created isolated WS33 branches. They do not authorize canonical coverage promotion. Before starting any packet, verify the named branch has not advanced; if it has, preserve its work and use its new exact HEAD/TREE rather than resetting it.

Common inspected isolated-branch identity at packet creation:

```text
HEAD = 895240f4058076764227a418ad28e84f61d3a7ed
TREE = cec73ae51b0168280ea648892f3e9edd46dcd883
FORGE_PIN = 8c7e9afb8e6caee88644b94e25da5852e36f8928
CANONICAL_WS33_HEAD = 6da237b704ba5e66c58c2334f47e36ef68ca980d
CANONICAL_WS33_TREE = 8f6d0151e4a9095c95925d6f6065214d382bf1dc
CANONICAL_PASS = 488
CANONICAL_UNKNOWN = 3700
```

Global rules for every packet:

- exact branch ownership only;
- materialize/qualify only current-UNKNOWN paths in the assigned family;
- no independent canonical coverage mutation or promotion;
- no requalification of G/H merely because older handoffs list them;
- no PASS inheritance from historical model identities without current compatibility evidence;
- actual-card witnesses must execute pinned Forge through the required production-reachable path;
- no card-name production hacks, manual outcome injection, direct-effect shortcuts, pilot-side legality, or silent fallbacks;
- hidden/RNG/replay/failure contracts remain authoritative;
- persist exact commands, evidence, source identities, and terminal handoff;
- if a real systemic engine/model defect is discovered, stop broad campaign expansion, isolate the minimal reproducer, classify P0–P5, and route back through Sol planning before a large repair.

---

## A — Trigger closure

```text
TASK_ID = WS33-A-TRIGGER-CLOSURE-20260908
MODEL_ROUTE = TERRA
TARGET_BRANCH = work/ws33-a-trigger-closure-20260907
INSPECTED_HEAD = 895240f4058076764227a418ad28e84f61d3a7ed
INSPECTED_TREE = cec73ae51b0168280ea648892f3e9edd46dcd883
CURRENT_CANONICAL_A_UNKNOWN = 57
```

### Goal

Materialize and qualify the remaining A-trigger frontier assigned to this isolated branch, using the current effective-path identity and current witness ABI, without touching the separate AF8 evidence-contract repair branch.

### Required execution

1. Verify branch HEAD/TREE and the canonical current A UNKNOWN set.
2. Derive a deterministic manifest containing only A-trigger paths still UNKNOWN under the current canonical model.
3. Partition by common production parent/runtime shape rather than card names.
4. Reuse existing compatible witness infrastructure only where exact current path identity and required evidence match.
5. Execute actual-card production-parent witnesses against pinned Forge.
6. Assert semantic state, decision/hidden/RNG evidence as required by each path.
7. Preserve standalone immutable shard evidence on this branch.
8. Do not modify canonical coverage.

### Escalation

Route back to normal Sol analysis if evidence suggests trigger ordering, replacement interaction, LKI, nested parent attribution, or a generic Forge trigger defect rather than a witness gap.

### Done

All branch-owned A-trigger current-UNKNOWN paths are either supported by complete branch-local evidence or remain explicitly UNKNOWN with a concrete blocker and root-cause classification.

---

## B — High-throughput amount/cost frontier

```text
TASK_ID = WS33-B-HIGH-THROUGHPUT-20260908
MODEL_ROUTE = TERRA
TARGET_BRANCH = work/ws33-b-high-throughput-20260907
INSPECTED_HEAD = 895240f4058076764227a418ad28e84f61d3a7ed
INSPECTED_TREE = cec73ae51b0168280ea648892f3e9edd46dcd883
CURRENT_CANONICAL_B_UNKNOWN = 675
```

### Goal

Close as much of the B current-UNKNOWN frontier as can be proven through bounded, systemic witness/campaign work, prioritizing high-throughput common shapes before hard Cost/Payment semantics.

### Required execution

1. Verify exact current B UNKNOWN identities from canonical inputs.
2. Cluster by actual consumer/runtime shape.
3. Start with amount/calculation and other state-observable high-throughput families whose semantics are already represented by the Rules Core.
4. Produce real actual-card witnesses with production parent execution and semantic postconditions.
5. Keep cost/payment choice legality authoritative in Forge; do not synthesize payment choices in the adapter.
6. If a cost/payment path requires a new external discretionary decision boundary, stop that cluster and emit a Sol-planning escalation packet instead of inventing one in Terra.
7. Preserve standalone branch evidence; no canonical coverage promotion.

### Hard-tail routing

```text
ordinary amount/cost witness gap -> TERRA
new authoritative Cost/Payment externalization or ambiguous payment semantics -> NORMAL_SOL -> TERRA -> NORMAL_SOL
```

### Done

High-throughput B paths owned by the branch are evidenced or precisely classified; hard-tail externalization gaps are isolated rather than papered over.

---

## C — Parent/SubAbility/API frontier

```text
TASK_ID = WS33-C-HIGH-THROUGHPUT-20260908
MODEL_ROUTE = TERRA
TARGET_BRANCH = work/ws33-c-high-throughput-20260907
INSPECTED_HEAD = 895240f4058076764227a418ad28e84f61d3a7ed
INSPECTED_TREE = cec73ae51b0168280ea648892f3e9edd46dcd883
CURRENT_CANONICAL_C_UNKNOWN = 700
```

### Goal

Qualify current-UNKNOWN C paths through the actual source-bound parent -> SubAbility/API production route, not by directly invoking standalone subabilities.

### Required execution

1. Recompute branch scope from current canonical UNKNOWN identities.
2. Cluster by parent consumer and child API shape.
3. Build production-parent witnesses that prove the target child/subability is reached and executes under the exact current path identity.
4. Retain parent/source provenance and target child reachability evidence.
5. Assert resulting semantic state and required decisions.
6. Reject any witness that succeeds only through direct `AbilitySub`/effect invocation outside the real parent route.
7. Keep global registries/canonical coverage untouched.

### Escalation

If a common parent cannot expose/resolve its child through the actual engine path, isolate whether this is P0 model attribution, P1 witness reachability, P2 adapter wiring, or P3 engine behavior before repair.

### Done

Every branch-owned C path has production-parent evidence or an explicit non-PASS classification with reproducer.

---

## D — High-throughput frontier

```text
TASK_ID = WS33-D-HIGH-THROUGHPUT-20260908
MODEL_ROUTE = TERRA
TARGET_BRANCH = work/ws33-d-high-throughput-20260907
INSPECTED_HEAD = 895240f4058076764227a418ad28e84f61d3a7ed
INSPECTED_TREE = cec73ae51b0168280ea648892f3e9edd46dcd883
CURRENT_CANONICAL_D_UNKNOWN = 920
```

### Goal

Materialize and qualify the highest-throughput common runtime shapes in D while preserving exact current path identity and branch-local evidence.

### Required execution

1. Derive the exact branch-owned current-UNKNOWN D manifest from canonical state.
2. Rank clusters by common runtime subsystem/consumer and expected evidence reuse, not by card count alone.
3. Prefer state-only/common action shapes before decision-heavy tails.
4. Execute pinned-Forge actual-card witnesses through real parent/runtime entrypoints.
5. Add generic regression infrastructure only when it represents a common engine shape.
6. Do not infer a systemic engine gap from unqualified paths alone.
7. Persist a machine-readable status partition of PASS-evidenced vs remaining UNKNOWN/blocker paths; do not promote canonical coverage.

### Escalation

Decision-heavy, hidden, replay, or rules-ambiguous tails require a new scoped Sol analysis packet rather than broadening this Terra task.

### Done

The branch has maximized safe high-throughput D evidence and leaves a sharply reduced, classified hard tail.

---

## E — Zone/trigger/replacement frontier

```text
TASK_ID = WS33-E-HIGH-THROUGHPUT-20260908
MODEL_ROUTE = TERRA_FOR_SIMPLE_FAMILIES
TARGET_BRANCH = work/ws33-e-high-throughput-20260907
INSPECTED_HEAD = 895240f4058076764227a418ad28e84f61d3a7ed
INSPECTED_TREE = cec73ae51b0168280ea648892f3e9edd46dcd883
CURRENT_CANONICAL_E_UNKNOWN = 1029
```

### Goal

Close simple, high-throughput E families through actual-card witnesses while explicitly separating the Replacement/LKI hard tail for Sol-assisted adjudication.

### Terra scope

Prioritize common simple shapes such as ordinary zone movement, trigger-observable state transitions, token creation, destroy/state outcomes, and other paths whose expected semantics are already unambiguous in current contracts.

For each cluster:

1. use the real production parent/runtime path;
2. assert pre/post semantic state;
3. preserve trigger/zone provenance where relevant;
4. retain decisions/replay evidence where required;
5. keep replacement ordering and LKI semantics out of heuristic adapter logic.

### Mandatory hard-tail split

If a path materially depends on replacement ordering, competing replacement effects, LKI, event substitution, or similarly cross-cutting rules semantics:

```text
ROUTE = NORMAL_SOL -> TERRA -> NORMAL_SOL
```

Do not solve that hard tail by generalizing from Forge behavior alone.

### Done

Simple E families are branch-evidenced; Replacement/LKI paths are isolated into explicit Sol-reviewable clusters with no false PASS.

---

## F — Static/pump/layers/copy/control frontier

```text
TASK_ID = WS33-F-HIGH-THROUGHPUT-20260908
MODEL_ROUTE = TERRA_FOR_SIMPLE_FAMILIES
TARGET_BRANCH = work/ws33-f-high-throughput-20260907
INSPECTED_HEAD = 895240f4058076764227a418ad28e84f61d3a7ed
INSPECTED_TREE = cec73ae51b0168280ea648892f3e9edd46dcd883
CURRENT_CANONICAL_F_UNKNOWN = 319
```

### Goal

Close simple F current-UNKNOWN families while separating true Layers/Copy/Control complexity from routine witness gaps.

### Terra scope

Prioritize simple pump/static outcomes and other directly state-verifiable continuous-effect shapes whose semantics are already fixed by current rules contracts.

Requirements:

- actual source-bound runtime execution;
- state assertions after the correct layer/application point;
- no direct effect invocation as substitute for parent reachability;
- no card-name special cases;
- historical WS29 evidence may inform tests but does not automatically qualify current effective-path identities.

### Mandatory hard-tail split

For copyable values, layer ordering/dependency, control-changing interactions, text/type/color-changing interactions, or other cross-layer semantics:

```text
ROUTE = NORMAL_SOL -> TERRA -> NORMAL_SOL
```

Current Forge behavior is evidence, not official-rules authority.

### Done

Simple F paths are evidenced; remaining Layers/Copy/Control paths are explicitly clustered with exact semantics/questions for Sol adjudication.

---

# Shared terminal handoff format

Every branch must finish with:

```text
BRANCH =
AUDIT_BASE_SHA =
FINAL_HEAD =
FINAL_TREE =
FORGE_PIN =
OWNED_PATH_COUNT =
EVIDENCED_PATH_COUNT =
REMAINING_UNKNOWN_COUNT =
ROOT_CAUSE_CLASSES =
TESTS_RUN =
TEST_RESULTS =
RUNS =
JOBS =
ARTIFACTS =
ARTIFACT_DIGESTS =
COVERAGE_MUTATED = FALSE
COVERAGE_PROMOTED = FALSE
PASS_EVIDENCE_INVALIDATED = TRUE/FALSE/UNKNOWN
SOL_REVIEW_REQUIRED = TRUE/FALSE
SOL_CODEX_REQUIRED = TRUE/FALSE
BLOCKERS =
NEXT_SERIAL_INTEGRATION_ACTION =
```

No branch may claim global WS33 completion.