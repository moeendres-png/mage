# ABC-D4a staticeffect run 34406229809 — FAIL (adjudicated)

Date: 2026-09-09. Branch: `work/ws33-d-high-throughput-20260907`.
TERMINAL_RESULT=FAIL. COVERAGE_PROMOTION=FALSE. COVERAGE_MUTATED=FALSE.
D-local evidence unchanged: 25 evidenced / 895 UNKNOWN. All 49 D4 paths
remain UNKNOWN; zero qualification credit from this run.

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34406229809; run head_sha `49e597b6522a8f977af20088e93b9a09ba7aea5e`
  == frozen SOURCE_HEAD == SOURCE_TREE `978a8f156ca49cb80dc071f462dc5d2a473fd967`.
- JOB=102649752014 (`d4-staticeffect`, completed failure).
- Failed step: `Execute ABC-D4a Record`. All prior steps PASS
  (model artifact, queue/ledger binding 49/49, pins, overlays,
  materialization 8+41 with TARGET_DIGEST `5094aa91...` reproduced).
- ARTIFACT=`ws33-abc-d4-staticeffect-34406229809` downloaded (contains
  `runtime/d4-record-runtime.log` with the failure evidence below).
- No record markers, no diagnostics, no campaign index produced.
  Certifier never ran. No per-path verdict exists.

## Root-cause classification: HARNESS_COMPILE_ERROR (implementation defect)

`Ws33D4StaticeffectCampaignTest.java` fails `javac` with 40+ instances
of `cannot find symbol: variable acc` (first at [613,34]):
`assertRecipePostconditionsInto` declares its accumulator parameter as
`assertions` while the method body (bulk-renamed during repair) refers
to `acc`. Pure local-variable naming defect introduced by the D4a
harness repair; no Forge API, semantic, fixture, or contract issue is
implicated. The defect escaped local gates because `javac` without Forge
dependencies cannot resolve symbols (environmental), and the
brace/method cross-checks did not catch a same-type rename.

## Repair plan (replacement run, same frozen scope)

One-line repair: rename the parameter to `acc`. No recipe, fixture,
pool, assertion, digest, pin, or contract change. Replacement run keeps
the identical frozen 8-path target set and TARGET_DIGEST.

## Exact next action

Commit FAIL checkpoint, apply the one-line repair, push (replacement run
triggers on the repair HEAD), register replacement PENDING.
