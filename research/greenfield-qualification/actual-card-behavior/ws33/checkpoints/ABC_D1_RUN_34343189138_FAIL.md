# ABC-D1 lifegain run 34343189138 — FAIL (adjudicated, root-caused)

Date: 2026-09-09. TERMINAL_RESULT=FAIL. COVERAGE_PROMOTION=FALSE.
COVERAGE_MUTATED=FALSE. No repartition (EVIDENCED stays 0/920).

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34343189138; JOB terminal (`d1-lifegain`, completed failure).
- SOURCE_HEAD=`6bd85b0039c63d4765a6bef2655604120058dcb2` == run head_sha.
- ARTIFACT=10100703957 (`ws33-abc-d1-lifegain-34343189138`, expired=false).
- GitHub ZIP digest `sha256:142108e4330e7a69d05be1bd6263554a7059e0271324f786c7d579b78df97878`
  reproduced locally (equal).
- Steps 1-12 PASS (incl. 13-col TSV materialization with intents).
  Step 13 Record FAILED at `testCompile`: line 138 `cannot find
  symbol: variable designated` (use-before-declaration: Provider
  construction precedes the designation resolution). No records, no
  diagnostics file, steps 14-15 skipped, step 16 upload PASS.

## Root-cause classification

D1_HARNESS_COMPILE_ERROR (harness-side ordering slip in new D1e code;
same family as the D1a import typo). No design fault: intent protocol,
resolution evidence, and certifier checks stand as designed. The D1e
intent rules themselves remain UNEXECUTED (no runtime evidence yet).

## Repair

Move `buildFixture` + `resolveDesignation` above controller/Provider
creation in `executeCase` (single hunk, zero semantic change to the
protocol). Local javac review: no non-dependency errors remain.

## Exact next action

Push repair (auto-triggers D1f run); register PENDING; adjudicate.
D1a/b/c/d/e witnesses stay immutable and unreused.
