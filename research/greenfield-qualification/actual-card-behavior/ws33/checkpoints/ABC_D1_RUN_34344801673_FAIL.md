# ABC-D1 lifegain run 34344801673 — FAIL (adjudicated, root-caused)

Date: 2026-09-09 (post-restart resume). TERMINAL_RESULT=FAIL.
COVERAGE_PROMOTION=FALSE. COVERAGE_MUTATED=FALSE.
No repartition (D_EVIDENCED stays 0). All 10 D1 paths remain UNKNOWN.

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34344801673; JOB=102443753023 (`d1-lifegain`, completed failure).
- RUN SOURCE HEAD=`747e13ba8801fc819fff6a640f431581b011831d` == run head_sha.
- ARTIFACT=10101338891 (`ws33-abc-d1-lifegain-34344801673`), size 9191040 bytes.
- GitHub ZIP digest `sha256:940f828180217b9140dc80a7d95c5b1a6923db70489c884262803f7fa05807c4`
  reproduced locally this session (equal).
- Steps 1-12 PASS (checkout, model artifact, queue/ledger binding,
  pins, all overlays, materialization 10+12, test installation).
  Step 13 Record FAILED (TestNG green, 6/10 markers, 4-entry
  diagnostics → gate correctly failed closed). Steps 14-15 skipped;
  step 16 upload PASS (this adjudication's basis).

## D1f contract verification (CODE_DERIVED from current source)

D1f source contains exactly the intended narrow ordering repair
(`buildFixture` + `resolveDesignation` before controller/Provider
creation, test lines 136-142) with the D1e intent protocol,
resolutions, certifier, selection, pins, and digests unchanged.
Retained as designed; no protocol redesign needed.

## Results

EVIDENCED-CANDIDATE (Record-only, NOT evidence, must not promote):
Angel's Mercy `...68ed887c...`, Avenge `...8d6bcdb...`,
Timely Reinforcements `...fe38d2d9...`, Cloudblazer `...f769bbd7...`,
Pizzasaur `...65b0bdd1...` (NEW: CONFIRM protocol verified),
Tainted Sigil `...c7d0546b...` (NEW: CONFIRM protocol verified).
Remaining UNKNOWN (4, all ENTITY cost consultations): Zuran Orb
`...3eee2ee6...`, Ayli `...58b84c12...`, Trading Post `...75d9c6fb...`,
Vampiric Rites `...957c83a8...`.

## First causal failure (from immutable artifact evidence)

All 4 failures: `ExternalDecisionValidationException:
UNSUPPORTED_DECISION_PATH: external decision provider failed` in
production cost payment. Per-case `decision-requests.jsonl` (new D1d
observability) shows the exact shape, identical in all 4 cases:

- kind `ENTITY_LIST_SELECTION`, min=max=1, cancel_allowed=false,
  response_schema discrete-selection.v1;
- options: `choice:0` DISCRETE `CANCEL` (entity_backed=false) +
  `choice:1` DISCRETE `ENTITY:card:N` (entity_backed=false),
  N = the fixture-designated entity's runtime id in each case.

The D1e provider required `option.isEntityBacked()` for the entity
option. The retained WS01 bridge models entity selection as
DISCRETE-typed options carrying `ENTITY:card:<id>` semantic values, so
the predicate matched zero options and the provider threw fail-closed;
the overlay re-wrapped it as UNSUPPORTED_DECISION_PATH.

## Classification

PROVIDER / DECISION_BOUNDARY (D-local provider binding predicate).
Not HARNESS fixture (fixtures/designations correct — the designated
entity IS the offered one in all 4 cases). Not FORGE_ENGINE (engine
offered the single forced option correctly). Not RULES_BEHAVIOR,
ENVIRONMENT, or shared-infrastructure defect: the bridge behaved per
contract; no retained Decision code needs mutation from D.
No SHARED_DECISION_BOUNDARY_HANDOFF required.

Fail-closed behavior held (only fully-asserted cases marked; the 4
failures carry exact request evidence for the next repair).

## Approved narrow repair (D1g)

Match the entity-denoting option by semantic value
`"ENTITY:" + optionIdFor(designated)` instead of `isEntityBacked()`:
require exactly one such option; require every other option to be
DISCRETE `CANCEL` with cancel disallowed and min>=1; select the
designated option. Game-object-id matching (not card-name matching),
no silent index picks, no outcome injection, everything else unchanged.

## Exact next action

Apply D1g provider-predicate repair; local validation (py syntax,
javac review with zero non-dependency errors, preparer/filter dry-run
against pinned scripts); commit FAIL checkpoint + repair separately;
push (digest unchanged, workflow unmodified → auto-run); register
PENDING; adjudicate.
