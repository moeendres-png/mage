# ABC-D3 damageall run 34378207214 — FAIL (adjudicated, root-caused)

Date: 2026-09-09. TERMINAL_RESULT=FAIL. COVERAGE_PROMOTION=FALSE.
COVERAGE_MUTATED=FALSE. No repartition (D_EVIDENCED stays 18).

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34378207214; JOB terminal (`d3-damageall`, completed failure).
- RUN SOURCE HEAD=`e64f16b1e83554b8544908b08a90e6976df91af9` == run head_sha.
- ARTIFACT=10114886563 (`ws33-abc-d3-damageall-34378207214`).
- GitHub ZIP digest `sha256:6e9096e2934721340f2c430d25091517f10303d59b30776b162696ff78609974`
  reproduced locally (equal).
- Steps 1-12 PASS. Step 13 Record: 3/7 markers, 4-entry diagnostics
  → gate correctly failed closed. Steps 14-15 skipped; step 16 upload
  PASS (this adjudication's basis).

## Results

EVIDENCED-CANDIDATE (Record-only, NOT evidence): Crystal
`...1199e731...`, Magma Phoenix (D3 path) `...21ee81f2...`, Y'shtola
`...2b7a04f8...` — all trigger machinery (SpellCast/dies/ETB,
settled loops, fixture casts) verified working. Remaining UNKNOWN (4):
Village Pillagers, Volcanic Torrent, Seismic Rupture, Chain Reaction.

## First-causal-failure analysis (from artifact + engine source)

All 4 failures: DamageAll spells resolve (spell in GY, pool empty,
lives exactly as predicted) but damaged creatures never reach the
graveyard (zone dumps show them alive on BF). Players take DamageAll
damage correctly (phoenix/yshtola/crystal lives exact), so resolution
occurred; only creature DEATH is missing.

Engine source at pin (`DamageAllEffect.resolve`): builds the damage
map for creatures AND players, calls `dealDamage`, then
`replaceDying`. Player life changes apply synchronously; creature
death requires a follow-up state-based-actions pass. The harness
`playUntilStackClear` loop exits the moment the stack empties, before
the next SBA pass moves lethally-damaged creatures to GY. Prior D1/D2
batches never hit this (zone-change destroys and life/draw/counter
effects need no SBA follow-up).

Classification: HARNESS driver defect (missing production SBA pump),
systemic across all damage-to-creature cases. Not engine/Rules/bridge/
fixture/pin fault. Repair: call the production SBA entry
`game.getAction().checkStateEffects(true)` (the same call PhaseHandler
itself uses) after the resolution loop, before asserting final zones.

## D-local evidence-limitation record (honest correction)

Re-examination triggered by this finding cleared ALL D1/D2 evidenced
paths: none has a creature-damage leg (D1 avenge destroy asserted 3
bears in GY via zone-change; D2 caustic/grave/shimmercreep/urborg legs
are player-life/draw exact; tainted-zero observed its amount function
at 0). No D1/D2 repartition change.

One D3 CANDIDATE needs a recorded limitation: Magma Phoenix
`...21ee81f2...` — Wrath necessarily kills all witness creatures
before the dies-trigger resolves, so its creature-damage leg is
VACUOUS-IN-FIXTURE (player leg -3/-3 exact + trigger/stack/resolution
proven). No state-only observer exists (mass-destroy kills all
witnesses; protection does not stop destroy-all; damage marks are
unassertable; targeted/combat kill fixtures belong to D6/D8).
Recorded in `WS33D_EVIDENCE_QUALIFICATIONS.json`: if the path is
evidenced, its creature leg is flagged for a same-path supplemental
case (D6 targeted-kill fixture) when that machinery exists. No
canonical claim is affected (D-local only, never promoted).

## Exact next action

Apply SBA-pump repair to the D3 driver; persist qualifications file;
dry-run; push (auto-triggers D3 replacement); register PENDING;
adjudicate. TARGET_DIGEST unchanged.
