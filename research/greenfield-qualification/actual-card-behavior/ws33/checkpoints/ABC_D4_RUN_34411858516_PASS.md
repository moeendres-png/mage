# ABC-D4a staticeffect run 34411858516 — PASS (adjudicated)

Date: 2026-09-09. TERMINAL_RESULT=PASS. COVERAGE_PROMOTION=FALSE
(D-local evidencing only). COVERAGE_MUTATED=FALSE.

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34411858516; JOB success (`d4-staticeffect`).
- RUN SOURCE HEAD=`94c37c2e90c03f0c1b34ea1c86a3ba89f7d5d6ab` == run head_sha;
  gate source_head/tree match (`458f82fd...`).
- ARTIFACT=10127673535 (`ws33-abc-d4-staticeffect-34411858516`).
- GitHub ZIP digest `sha256:53b58d091fba1e28dffffa1dc4b91cfa8a8c8b89e782e4f81db165b87f8c265b`
  reproduced locally (equal).
- Internal `ABC_D4_HASHES.sha256`: 96 entries, all verify, zero bad.
- Gate `ABC_D4_GATE.json`: PASS, 8 paths, forge pin `8c7e9afb...`,
  target `5094aa91...`, 0 decisions (all NONE-intent, pools exact),
  0 RNG, 0 leaks, 0 divergence.
- Diagnostics 0 bytes each; index 8 records.
- Per-path 8/8: markers; EXTERNALLY_RULE_VALIDATED records, all
  assertions PASS (incl. `effect-command-present` + `effect-kind-*`),
  deltas equal; traces bound with effect_kind (5 static / 2 replacement
  / 1 trigger); tapes vacuous (0 events, consistent with NONE intents);
  observation PASS; replay divergence 0.
- Consequence highlights: Silence probe-baseline GY + probe-rejected
  hand (rejection proven, mechanism self-validated); Burning H0
  confirmed (unpaid blight exiles exactly 2); Gideon loyalty 6 with
  command-zone `Gideon Jura (3)'s Effect` + Seismic in GY; Seaweed
  hawk-kill cascade; Memnite survival under Ethersworn prevention;
  kicker-skip on Unstable Footing.
- Anti-double-credit: 8 ∩ partition-evidenced-25 = 0; ∩ retained-PASS
  `ede58d66` = 0; ∩ non-UNKNOWN ledger = 0.
- Predecessor FAILs (34406229809, 34407599333, 34409161259) classified
  before repair; cancelled duplicate 34409164024 never adjudicated.

## Evidenced set (D-local, 8 new)

236a197c (Silence, CantBeCast probe), 48c858c0 (Ethersworn, Memnite
prevention), 4a791d62 (Gideon L11, cast-entry + loyalty-6 prevention),
6621cb87 (Burning Curiosity, H0 exile-2), 71482ff8 (Hildibrand,
dies MayPlay-self), 7a54c6e5 (Unstable Footing, unkicked static),
a907c974 (Electric Seaweed, die-trigger cascade), ffe5f7ee
(Leitmotif Composer, sole-AB static).

## Standing rules established (carry to D4b-f / D5)

- Planeswalkers enter only via production CAST (direct BF placement
  skips loyalty entry; verified: no loyalty placement in
  `PlayerZoneBattlefield`, SBA kill in `handlePlaneswalkerRule`).
- Probe designs must not place duplicate hand copies under a uniqueness
  guard (return-first for identical copies).
- Pin-derived API stub compile is a required local gate for harness
  changes (proven sensitive: reproduces exact CI errors).

## D partition after repartition

EVIDENCED=33, REMAINING_UNKNOWN=887 (incl. 1 template-missing blocked).

## Exact next action

D4b emblem setup batch (tezzeret/elspeth/chandra ultimates, D5 family,
production-cast entry per standing rule) — per-path survey first if the
emblem setup shape differs per card; then D4c/D6, D4d/D7, D4e/D8, D4f
review, D5 tail per ranked plan.
