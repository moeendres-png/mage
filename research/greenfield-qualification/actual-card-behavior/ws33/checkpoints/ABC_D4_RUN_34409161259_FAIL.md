# ABC-D4a staticeffect run 34409161259 — FAIL (adjudicated)

Date: 2026-09-09. Branch: `work/ws33-d-high-throughput-20260907`.
TERMINAL_RESULT=FAIL. COVERAGE_PROMOTION=FALSE. COVERAGE_MUTATED=FALSE.
D-local evidence unchanged: 25 evidenced / 895 UNKNOWN. All 49 D4 paths
remain UNKNOWN; zero qualification credit from this run.

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34409161259; run head_sha `f658130cb1db3fca089d3d81e4da87c4d2efb885`
  == frozen SOURCE_HEAD. JOB completed failure at `Execute ABC-D4a
  Record`; all prior steps PASS (materialization 8+41, TARGET_DIGEST
  `5094aa91...`).
- ARTIFACT=`ws33-abc-d4-staticeffect-34409161259` downloaded.
- Record result: 6/8 cases SUCCESS (ethersworn, hildibrand, leitmotif,
  burning with H0 exile-2 confirmed, unstable kicker-skip confirmed,
  seaweed cascade confirmed), 2 fail-closed with exact diagnostics:
  - Silence `236a197c`: `hand card not unique: Dark Ritual`.
  - Gideon `4a791d62`: `assertion host-battlefield mismatch actual=0`,
    Gideon in GY, command zone empty, everything else settled normally.
- Duplicate run 34409164024 stayed CANCELLED (not evidence).

## Root-cause classification (both harness/fixture defects, not semantic)

1. SILENCE — FIXTURE_AMBIGUITY: `findHandCard` demands uniqueness but the
   probe design places 2 identical Rituals upfront, so Phase A already
   throws. Fail-closed worked as designed. Repair: return-first match
   (identical copies; hand order deterministic across record/replay).
2. GIDEON — FIXTURE_ENTRY_DEFECT: the host was placed via direct
   `zone.add` (`AITest.addCardToZone`), which bypasses the production
   entry pipeline. `PlayerZoneBattlefield` contains no loyalty-counter
   placement (verified at pin); `GameAction.handlePlaneswalkerRule`
   sends loyalty-0 planeswalkers to GY at the first SBA check — exactly
   the observed dump. Production-cast planeswalkers receive loyalty via
   `moveTo`. Repair: enter via production CAST (3WW; pool becomes
   `W2R1C5` with fixture Seismic), never via direct BF placement.
   STANDING D-HARNESS RULE: planeswalkers enter only via production
   cast (also governs future D4b emblem work).

## Repair plan (third replacement run, same frozen scope)

Preparer pool fix + harness `findHandCard` return-first + ANIMATE recipe
cast-entry. No path-set, digest, pin, or contract change. Same frozen
8-path target set and TARGET_DIGEST.

## Exact next action

Commit FAIL checkpoint, apply repairs, re-run stub-compile + dry-run
gates, push (replacement run triggers on the repair HEAD), register
replacement PENDING.
