# ABC-D4b emblem run 34418252401 — FAIL (adjudicated)

Date: 2026-09-10. Branch: `work/ws33-d-high-throughput-20260907`.
TERMINAL_RESULT=FAIL. COVERAGE_PROMOTION=FALSE. COVERAGE_MUTATED=FALSE.
D-local evidence unchanged: 33 evidenced / 887 UNKNOWN. All 3 D4b paths
remain UNKNOWN; zero qualification credit from this run.

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34418252401; run head_sha `9ab2b7f8902d78e01bbcc8db83e982b4ae85771d`
  == frozen SOURCE_HEAD (`c2b41027…` tree). JOB `d4b-emblem` 102687841126
  conclusion failure at `Certify exact per-path evidence`; all prior steps
  PASS (model artifact, queue binding 49, pins, overlays, materialization
  3+46, TARGET_DIGEST `0b856b3c…`, Record 3/3 markers with empty
  diagnostics, tape-driven Replay index 3 with empty diagnostics).
- ARTIFACT=10130045658 (`ws33-abc-d4b-emblem-34418252401`) downloaded and
  unpacked locally for per-path adjudication.
  GitHub digest `sha256:9e986c2b860be3e22d2fd1db00306c0425225de04ace809bc4440331002f4bc3`.
- Runtime evidence per path (record + trace + tapes + observation + replay,
  all present, all internally consistent):
  - Tezzeret `0006052e…`: 10/10 assertions PASS incl `entry-loyalty-8`,
    `effect-command-present`, `effect-kind-trigger-present`,
    `final-loyalty-2`; trace entry_loyalty=8, kind=trigger, 0 decisions,
    0 RNG, 0 leaks; tape 0 events; observation PASS; replay divergence 0.
  - Elspeth `2d5a6d01…`: 11/11 PASS incl `entry-loyalty-8`,
    `effect-command-present`, `effect-kind-static-present`,
    `host-graveyard`, `bear-battlefield`, `destroyer-graveyard`;
    trace entry_loyalty=8, kind=static, 0/0/0; observation PASS;
    replay divergence 0.
  - Chandra `96a93b32…`: 10/10 PASS incl `entry-loyalty-8`,
    `effect-command-present`, `effect-kind-trigger-present`,
    `final-loyalty-1`; trace entry_loyalty=8, kind=trigger, 0/0/0;
    observation PASS; replay divergence 0.
  The engine-owned Doubling Season entry-doubling bridge (survey
  CODE_DERIVED) is corroborated by all three production casts reading
  exactly loyalty 8. No semantic defect is implicated anywhere.

## Root-cause classification: HARNESS_FAILURE (certifier defect, no semantic implication)

`ws33_certify_abc_d4b.py` line 72 raised
`UnboundLocalError: cannot access local variable 'trace'`: the
`entry_loyalty` gate assertion was placed before `trace.json` is loaded
(record-level block), so `trace` is unbound on the first path iteration.
One-line D4b-scope repair: move the `entry_loyalty` assertion after the
trace load. No preparer/filter/harness/workflow/pin/contract/path-set
change. The failed run is terminal; its records are NOT promoted and are
never reinterpreted. A replacement run on the repaired source is required.

## Repair plan (first replacement run, same frozen scope)

Certifier one-line reorder + py_compile + certifier negative self-test +
POSITIVE self-test of the repaired certifier against this run's downloaded
artifact (discarded gate) + preparer dry-run + digest stability +
stub-compile reconfirmation (Java harness untouched). Same frozen 3-path
target set and TARGET_DIGEST. Then push (replacement run triggers on the
repair HEAD), register replacement PENDING.

## Exact next action

Commit this FAIL checkpoint, apply the certifier repair, re-run all
pre-run gates, push, register replacement PENDING. Do not start D4c.
