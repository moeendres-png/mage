# ABC-D1 lifegain run 34341241374 — FAIL (adjudicated, root-caused)

Date: 2026-09-09. TERMINAL_RESULT=FAIL. COVERAGE_PROMOTION=FALSE.
COVERAGE_MUTATED=FALSE. No repartition (EVIDENCED stays 0/920).

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34341241374; JOB=102432266197 (`d1-lifegain`, completed failure).
- SOURCE_HEAD=`19cdb2fea70bcfacffb50497418ce2a58b93a3dd` == run head_sha.
- ARTIFACT=10099937862 (`ws33-abc-d1-lifegain-34341241374`, expired=false).
- GitHub ZIP digest `sha256:5bf949c2f9b01a38121e1e0f22139882f2aa9a4253198c9512640abf3c696ea0`
  reproduced locally (equal).
- Steps 1-12 PASS. Step 13 Record: TestNG green, 4/10 markers,
  6-entry diagnostics → gate correctly failed closed. 14-15 skipped;
  16 upload PASS (this adjudication's basis).

## Results

EVIDENCED-CANDIDATE (Record-only, NOT evidence, must not promote):
- Angel's Mercy `...68ed887c...`, Avenge `...8d6bcdb...`,
  Timely Reinforcements `...fe38d2d9...` (carried from D1c shapes),
  Cloudblazer `...f769bbd7...` (NEW: hand==2/library==3 fix verified).
Remaining UNKNOWN (6): Zuran Orb, Ayli, Pizzasaur, Trading Post,
Vampiric Rites, Tainted Sigil — all ACTIVATED, all cost-payment
consultations. Cloudblazer assertion defect F2 CLOSED by this run.

## Decoded request shapes (D1d observability payoff, DIRECTLY_VERIFIED
from per-case `decision-requests.jsonl`, 1 request per failing case)

- Sac-other/discard costs (Zuran/Ayli/TradingPost/Vampiric):
  `ENTITY_LIST_SELECTION`, min=max=1, cancel_allowed=false,
  options=[CANCEL(choice:0), ENTITY:card:N(choice:1)] — exactly one
  entity-backed option alongside a disallowed CANCEL.
- Sac-self costs (Pizzasaur/TaintedSigil): `CONFIRM_PAYMENT`,
  min=max=1, cancel_allowed=false, options=[true,false].

## Root-cause classification (refined)

F1 (provider protocol gap, harness-side): provider only answered
single-option requests; cost consultations carry the CANCEL/true-false
companion option. No Rules-Core/overlay/pin/selection fault. No shared
Decision infrastructure defect indicated: the bridge behaved exactly
per contract (authoritative options + bounds + cancel flag); the gap is
D-harness-side (no intent-scripted answers yet).

## D1e protocol (approved for implementation)

- ENTITY_LIST_SELECTION: case-declared intent designates the exact
  fixture entity (resolved to the runtime Card object;
  `optionIdFor` computed, never hard-coded); select it IFF it is the
  SOLE entity-backed option, CANCEL present only with
  cancel_allowed=false and min>=1. Forcedness: sole entity option.
- CONFIRM_PAYMENT {true,false}, cancel_allowed=false, min=max=1, on
  driver-initiated mandatory-cost activations: select `true` (unique
  completion path; `false` aborts the witnessed play or fails
  validation). Fully taped + replay-verified + resolution-recorded.
- Anything else: throw (fail closed). No card-name hard-coding as
  decisions; no silent index picks; no outcome injection.
- Certifier: every resolution forced==true, selected==expected,
  resolution count == tape event count, all ACCEPTED.

## Exact next action

Implement D1e (preparer intent column + plan intents; provider intent
rules + resolution summary in trace; certifier resolution checks),
validate locally, push (digest unchanged, workflow unmodified),
register PENDING, adjudicate.
