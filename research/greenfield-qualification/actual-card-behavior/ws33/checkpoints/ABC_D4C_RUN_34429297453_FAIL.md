# ABC-D4c single-target replacement run 34429297453 — FAIL (terminal, adjudicated)

Date: 2026-09-10. Branch: `work/ws33-d-high-throughput-20260907`.
TERMINAL_RESULT=FAIL. COVERAGE_PROMOTION=FALSE. No coverage change:
D-local evidence stays 36 / 884. All 4 T1 paths remain UNKNOWN. The single
authorized D4c replacement run is CONSUMED. No further D4c runs without
coordinator authority — the staged encoding repair below is COMMITTED
LOCALLY ONLY and NOT PUSHED (a push would auto-trigger an unauthorized
run via the workflow path filter).

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34429297453 (`WS33 ABC D4c target closure`), JOB `d4c-target`,
  conclusion failure. RUN SOURCE HEAD=`470ca9f70d…` == run head_sha ==
  frozen repair commit; no source drift.
- Boundary steps all PASS (checkout, model, queue/ledger 49, pins, WS01/
  WS12/WS32, overlays incl. target-selection, materialization 4+45 digest
  `1c16bf25…`). `Execute ABC-D4c Record` FAIL: 0/4; diagnostics non-empty.
- ARTIFACT=10133933530 (`ws33-abc-d4c-target-34429297453`), digest
  `sha256:7a7f23d5957b598f4ab351ceddc8391aa94b4b8cb96d347e582967e001ffd23e`.
- The registered-controller repair WORKED: all 4 cases now issue exactly
  one authoritative TARGET_SELECTION each (`decision-requests.jsonl`
  present in all 4 record dirs — the zero-request defect is cured).
- Per-case diagnostics (artifact): all 4
  `ExternalDecisionValidationException UNSUPPORTED_DECISION_PATH:
  external decision provider failed` (engine-side wrap of the provider's
  fail-closed throw; no silent credit).

## Root cause (HARNESS, option-encoding assumption, log-evidenced)

The provider matched the designated entity by presumed semantic string
`CARD:<id>` (borrowed from the older G-restrictions pattern). The
authoritative request encoding at this pin/overlay is DIFFERENT and
uniform across all 4 cases: exactly 2 entity-backed CARD options with
`option_id=card:<id>`, `entity_kind=CARD`, `entity_id=<long>`,
`semantic_value=<id-string>`, `min=max=1`, `cancel_allowed=true`, no DONE
option. Zero options matched `CARD:<id>` → unique-match gate correctly
threw on all 4. Engine targeting, fixture, overlay transport, and case
design are exonerated: the engine offered genuine 2-candidate discretion
in the exact predicted shape.

Engine-side observations recorded (no action): mandatory single targets
arrive with `cancel_allowed=true`; the provider never takes CANCEL (it
responds only with an entity option id; engine validates). No DONE
transition appears for mandatory single targets.

Classification: HARNESS (bounded provider-encoding defect). Not ENGINE,
not FIXTURE, not CERTIFIER, not INFRASTRUCTURE. Semantic target, case
set, digest, fixture, and contract unchanged.

## Staged repair (local only, validated statically, NOT pushed)

`Ws33D4cTargetCampaignTest.Provider` TARGET_SELECTION branch now matches
by native entity id (`option.isEntityBacked() &&
option.getEntityId() == designated.getId()`, exactly-one-or-fail-closed;
expected/selected binding via id string). Gates: pin-derived stub-compile
green (stubs extended with `dangerouslySetController`, `getId`,
`getCounters`, `CounterType`, `GameObject` hierarchy as needed); preparer/
filter/certifier/overlay anchors unaffected (re-verified unchanged files
byte-identical to the adjudicated run source except the harness). The
repair commit sits on this branch tip locally; remote HEAD remains
`bd90ee383b` (replacement-run PENDING). DO NOT PUSH without coordinator
run authority.

## Exact next action (coordinator decision required)

Authorize exactly one final D4c-T1 run (push of the staged repair), or
close D4c-T1 as failed-deferred and redirect. On an authorized run:
register PENDING against the pushed repair SHA, poll, adjudicate
per-path; PASS (4/4, 1 TARGET_SELECTION each, ACCEPTED, unique-match,
0 RNG, 0 leaks, 0 divergence) repartitions D to 40 evidenced / 880
remaining. No D4d/D4e/D4f/Batch-6/Full107 started from this workstream.
