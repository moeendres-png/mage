# ABC-D4a staticeffect run 34407599333 — FAIL (adjudicated)

Date: 2026-09-09. Branch: `work/ws33-d-high-throughput-20260907`.
TERMINAL_RESULT=FAIL. COVERAGE_PROMOTION=FALSE. COVERAGE_MUTATED=FALSE.
D-local evidence unchanged: 25 evidenced / 895 UNKNOWN. All 49 D4 paths
remain UNKNOWN; zero qualification credit from this run.

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34407599333; run head_sha `f05e00b4c107af9f5343218dc0ccee4d401803ac`
  == frozen SOURCE_HEAD == SOURCE_TREE `e44b80ff0cf98bba4bd2f8cb24e62e933d5383ae`.
- JOB conclusion failure at step `Execute ABC-D4a Record`. All prior
  steps PASS (materialization 8+41, TARGET_DIGEST `5094aa91...`).
- ARTIFACT=`ws33-abc-d4-staticeffect-34407599333` downloaded; failure
  evidence is `runtime/d4-record-runtime.log` (BUILD FAILURE, 1 error).
- No record markers, no diagnostics, no campaign index. Certifier never
  ran. No per-path verdict exists.

## Root-cause classification: HARNESS_COMPILE_ERROR, residue of prior repair

`Ws33D4StaticeffectCampaignTest.java:[681,16] cannot find symbol:
variable assertions` — `assertRecipePostconditionsInto` ends with
`return assertions;` while its accumulator parameter (renamed by the
previous one-line repair) is `acc`. Same rename-residue class as run
34406229809, one token remaining. No Forge API, semantic, fixture, or
contract issue implicated.

## Repair plan (second replacement run, same frozen scope)

One-token repair: `return assertions;` → `return acc;`, plus a
whole-file residue scan (`assertions`, `lastGame`, `half`, `isPrimary`
must be absent). No recipe/fixture/pool/assertion/digest/pin/contract
change. Identical frozen 8-path target set and TARGET_DIGEST.

## Exact next action

Commit FAIL checkpoint, apply repair + residue scan, push (replacement
run triggers on the repair HEAD), register replacement PENDING.
