# ABC-D1 lifegain run 34307867562 — FAIL (adjudicated, root-caused)

Date: 2026-09-09. TERMINAL_RESULT=FAIL. COVERAGE_PROMOTION=FALSE.
No repartition (EVIDENCED stays 0/920). No canonical mutation.

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34307867562; JOB=102328212899 (`d1-lifegain`, completed failure).
- ARTIFACT=10087359264 (`ws33-abc-d1-lifegain-34307867562`, expired=false).
- GitHub ZIP digest `sha256:db29103f1c7ff178a925f2e40540495c50dd6c87a1e51799e2cbfd252bf3a727`
  reproduced locally (`sha256sum` equal). 1185 ZIP entries readable.
- Source binding: run head_sha = SOURCE_HEAD `9b7622936c` (frozen).
- Steps 1-12 PASS: model artifact verified, queue/ledger binding
  (22/22, TARGET_DIGEST reproduced in-run), all four runtime pins,
  all seven overlays applied, preparer+filter materialization
  (10 cases + 12 deferred) PASS.
- Step 13 Record FAILED at `testCompile`: `Ws33D1LifegainCampaignTest`
  line 13 imports `forge.game.player.PlayerControllerHuman`, which does
  not exist. Evidence: `d1-record-runtime.log` (in-artifact) shows exactly
  4 compile errors, all `cannot find symbol: class PlayerControllerHuman
  in package forge.game.player`. Steps 14-15 skipped; step 16 artifact
  upload succeeded (this adjudication's basis).

## Root-cause classification

D1_HARNESS_COMPILE_ERROR (qualification-test-only import typo).
`PlayerControllerHuman` lives in `forge.player` (B1-proven import);
`PlaySpellAbility` lives in `forge.game.player` (correct as written).
No production, overlay, pin, selection, or evidence-design fault.
All D1 design facts (10+12 split, recipes, fixtures, contracts) stand.

## Repair (minimal, test-only, zero semantic effect on machinery)

One-line fix: `import forge.game.player.PlayerControllerHuman;` ->
`import forge.player.PlayerControllerHuman;` in
`Ws33D1LifegainCampaignTest.java`. No preparer/filter/certifier/
workflow/contract change. Replacement run D1b required; D1a artifact
remains an immutable failure witness (not reused as evidence).

## Exact next action

Push the import repair (re-triggers workflow as a new run); register a
new PENDING checkpoint for the replacement run; adjudicate on terminal.
