# ABC-D1 lifegain run 34339878305 — FAIL (adjudicated, root-caused)

Date: 2026-09-09. TERMINAL_RESULT=FAIL. COVERAGE_PROMOTION=FALSE.
COVERAGE_MUTATED=FALSE. No repartition (EVIDENCED stays 0/920).
All 10 D1 paths remain UNKNOWN.

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34339878305; JOB=102427903710 (`d1-lifegain`, completed failure).
- SOURCE_HEAD=`a697d61dfa7a7217b4a153a69cc159ca1055d153` == run head_sha.
- ARTIFACT=10099387942 (`ws33-abc-d1-lifegain-34339878305`, expired=false).
- GitHub ZIP digest `sha256:30f3f8837b0c6834d7c45a4f1353b1eae971b18a707726a6bcf023a2bf6d0a94`
  reproduced locally (equal). ZIP entries readable.
- Steps 1-12 PASS (incl. TARGET_DIGEST reproduced in-run with exact
  pinned `Name:` values). Step 13 Record: TestNG green
  (`Tests run: 1, Failures: 0`), 3/10 record-success markers,
  7-entry diagnostics → workflow gate correctly failed closed.
  Steps 14-15 skipped; step 16 upload PASS (this adjudication's basis).

## Partial-record status (NOT evidence)

3 SPELL records exist (angels_mercy, avenge, timely_reinforcements) but
Replay + certification never ran: NO path is evidenced. Per contract,
green Record alone grants nothing. UNKNOWN stays UNKNOWN.

## First-causal-failure analysis

R1/R2 repairs verified effective (exact names resolve; hosts placed;
3 forced-cost-free SPELL paths record fully). Two remaining defects,
both qualification-harness-side:

F1 (provider): all 6 ACTIVATED cases fail in production cost payment
with `UNSUPPORTED_DECISION_PATH: external decision provider failed`.
Mechanism (verified in WS01 patch): the overlay wraps any provider
throw. The provider threw because cost-selection requests carry != 1
option. Exact request shapes (kind/bounds/options) were swallowed by
the wrap — the harness logged no request detail. NOT a Rules-Core,
overlay, pin, or selection fault.

F2 (assertion): Cloudblazer ETB drew 2 into an emptied hand, so hand
must be 2, not 1 (`expected exactly one drawn card in hand` is a wrong
constant; library 5 -> 3 unasserted).

## Root-cause classification

D1_HARNESS_OBSERVABILITY_DEFECT (F1: request shapes unlogged) +
D1_HARNESS_ASSERTION_DEFECT (F2: wrong hand constant).
Fail-closed behavior held again (3 markers only for fully-asserted
cases; no false evidence).

## Exact next action

D1d: systemic request-detail capture (`decision-requests.jsonl` per
case: kind/bounds/options/context for EVERY request, answered or not)
+ cloudblazer hand==2/library==3 correction; re-run for observation;
then encode the observed forced cost-selection protocol explicitly
(fixture-designated intent + replay) in D1e. D1a/b/c witnesses stay
immutable and unreused.
