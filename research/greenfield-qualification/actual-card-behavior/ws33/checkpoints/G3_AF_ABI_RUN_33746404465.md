# WS33 G3 AF ABI run 33746404465 — adjudication checkpoint

Status: `FAILED_DIAGNOSTIC`

This checkpoint is intentionally persistent and resume-safe. It does not mutate WS33 coverage and it does not invalidate the already qualified 21/21 AF behavior artifact.

## Exact run identity

- branch: `work/ws33-g3-final-closure-20260902`
- source HEAD: `e3836275ee56e106e0ef822b5dcbe48cc0f69135`
- source TREE: `0288848917d62e5bfdc7e8210170ae7a7ad7e9ac`
- run: `33746404465`
- job: `100619768722`
- artifact: `9889890747`
- artifact digest: `sha256:9d9e9c2c108c322a75a1211ab5c8d1f93019f509663cb95c639c1602cabb7e6c`
- downloaded ZIP SHA256: `9d9e9c2c108c322a75a1211ab5c8d1f93019f509663cb95c639c1602cabb7e6c`

## First failure

The first material runtime failure is path:

`forge-behavior-v2:12825b1c31ca7d7d8a01cad16bc5531930cb5a7e`

The generated AF campaign records:

- parent dispatch: `RepeatEach`
- target implementation: `forge.game.ability.effects.DigEffect`
- stack admission: `1`
- stack resolution: `0`
- target execution observer: `1`
- failure type: `forge.gamemodes.match.input.ExternalDecisionValidationException`
- failure: `UNSUPPORTED_DECISION_PATH: hidden authoritative Card choices require RemoteClient principal observation`

All later `non-quiescent stack before exact path` rows are cascading failures after this unresolved first stack item and are not independent root causes.

A later `TwoPilesEffect` stack trace is likewise downstream and must not be treated as the first blocker.

## Differential evidence

The immutable focused AF behavior run `33745809012` on HEAD `28f4e7cdd6a35c488d4633cf8d77163a8aa2d5d9` is green for the same 21 exact cases:

- 21/21 behavior PASS
- 21/21 stack admission/resolution
- 21/21 target-SVar reachability
- the first path above has no decision events and has four observed target executions.

`git compare 28f4e7c...e383627` proves that the only changed files are:

1. `.github/workflows/ws33-g3-svar-af-abi-replay.yml` (new)
2. `WS33_CONTINUATION_HANDOFF.md`

No harness, decision-boundary, hidden-info, target-selection, SVar-reachability, Forge, or rules code changed.

The new ABI workflow differs from the green behavior workflow at the record invocation by co-running:

`forge.gamesimulationtests.Ws33TargetSelectionExternalizationTest`

and

`forge.net.Ws33GSVarAfQualificationTest`

inside the same Maven/Surefire execution. The green AF behavior run executes only the AF campaign test.

## Classification

`CODE_DERIVED`: the ABI gate violates the process-isolation expectation for qualification by running an unrelated stateful Game/Controller regression test in the same test JVM immediately before/alongside the campaign. The first campaign path then sees a RemoteClient-principal-observation precondition failure that does not occur in the otherwise identical isolated AF campaign.

This is a qualification-workflow isolation defect, not evidence that the already-green AF rules behavior regressed.

## Atomic repair

1. Keep `Ws33TargetSelectionExternalizationTest` as a required regression gate, but execute it in its own Maven/Surefire invocation.
2. Execute `Ws33GSVarAfQualificationTest` in a fresh subsequent Maven/Surefire invocation, with only the campaign test selected.
3. Preserve all exact pins, overlays, cases, request instrumentation, fail-closed behavior, and artifact retention.
4. Do not add a fallback or relax the RemoteClient principal-observation guard.
5. Re-run only the AF ABI/replay workflow.
6. Acceptance remains: 21 campaign PASS, target reachability positive for all 21, Decision-required 9/9 observed, RNG-required 4/4 observed, record/replay request traces equal, tape-driven replay equal, no coverage mutation.

`WS33_COMPLETE = FALSE`
