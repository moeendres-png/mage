# ABC-D2 lifelose run 34366533583 — FAIL (adjudicated, root-caused)

Date: 2026-09-09. TERMINAL_RESULT=FAIL. COVERAGE_PROMOTION=FALSE.
COVERAGE_MUTATED=FALSE. No repartition (D_EVIDENCED stays 10).

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34366533583; JOB terminal (`d2-lifelose`, completed failure).
- RUN SOURCE HEAD=`83ce165ef94a0064051f98eae8f8b9e327bb771c` == run head_sha.
- ARTIFACT=10110266885 (`ws33-abc-d2-lifelose-34366533583`).
- GitHub ZIP digest `sha256:df67b20acc2827ea6806004661b47a24d3ed36032b4ca074959126f7643e809a`
  reproduced locally (equal).
- Steps 1-12 PASS. Step 13 Record: TestNG ran, 3/8 markers,
  5-entry diagnostics → gate correctly failed closed. Steps 14-15
  skipped; step 16 upload PASS (this adjudication's basis).

## Results

EVIDENCED-CANDIDATE (Record-only, NOT evidence): Night's Whisper
`...78a59b77...`, Urborg Syphon-Mage `...8f4f0eb8...` (ENTITY-discard
protocol transfers to LoseLife AB), Painful Truths `...e4b31edf...`
(converge X=2 from exact W+B spend). Remaining UNKNOWN (5).

## First-causal-failure analysis (per-case, from artifact evidence)

1. Caustic Hound + Grave Venerations (DIES_TRIGGER_WRATH): life deltas
   0 — the resolving destroyer leaves the stack momentarily empty, so
   `playUntilStackClear` exits before waiting dies-triggers transfer.
   HARNESS driver defect (missing settle loop); D1 never hit this
   (subability chains only, no mid-resolution triggers).
2. Shimmercreep (ETB_TRIGGER_X): X resolved as 1, expected 3 — FIXTURE
   defect with a rules lesson: basic lands are colorless, so
   `Count$Valid Permanent.YouCtrl$Colors` over Plains+Island+self(B)
   is 1, matching correct MTG color rules. Not an engine defect.
3. Circle of Power: life/draw correct; token assert used wrong name.
   HARNESS assertion defect: token script `b_0_1_wizard_snipe.txt`
   names it `Wizard Token` (verified at pin).
4. Vraska: host find failed with empty record dir; script verified
   clean ASCII single-faced. Cause UNKNOWN pending empirical probe:
   HARNESS observability gap (no BF listing on find failure).

Fail-closed behavior held throughout (3 markers only for fully
asserted cases).

## Approved repairs (D2c, all D-local narrow)

- Settled-loop driver for DIES_TRIGGER_WRATH (transfer rounds with cap).
- Shimmercreep fixture → Elite Vanguard + Grizzly Bears (true W/G/B).
- Token assert → `Wizard Token`.
- `requireHostOnBattlefield` with BF-name listing for host placements
  (Vraska probe; reused by D2 ACTIVATED driver).

## Exact next action

Push repairs (auto-triggers D2 replacement); register PENDING; adjudicate.
TARGET_DIGEST unchanged; workflow/test-loader/certifier untouched.
