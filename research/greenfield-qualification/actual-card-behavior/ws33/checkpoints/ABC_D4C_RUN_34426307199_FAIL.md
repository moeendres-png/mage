# ABC-D4c single-target run 34426307199 — FAIL (terminal, adjudicated)

Date: 2026-09-10. Branch: `work/ws33-d-high-throughput-20260907`.
TERMINAL_RESULT=FAIL. COVERAGE_PROMOTION=FALSE. No coverage change:
D-local evidence stays 36 / 884. All 4 T1 paths remain UNKNOWN. D4a/D4b
retained evidence untouched (see §4 for the inert-provider finding, which
does NOT requalify them).

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34426307199 (`WS33 ABC D4c target closure`), JOB `d4c-target`
  102712175382, conclusion failure, 3m41s.
- RUN SOURCE HEAD=`afed0066568fb7036eaec7f1ebf8692b3773bd50` == run
  head_sha == frozen machinery commit; no source drift.
- Boundary steps all PASS: source checkout, model artifact, queue/ledger
  binding (49), Forge pin, WS01/WS12/WS32 prerequisites, runtime overlays
  (incl. target-selection), case materialization (4+45, digest `1c16bf25…`).
- `Execute ABC-D4c Record` FAIL: 0/4 record cases; diagnostics
  `target-record-diagnostics.jsonl` non-empty (4 entries); no records dir.
- ARTIFACT=10132863096 (`ws33-abc-d4c-target-34426307199`), digest
  `sha256:291ed4820bf272ee5f0bd9c20756cf0fd46b8b0e00a671f8387b152be74ed7b3`.
- Per-case diagnostics (artifact): surge `engine did not retain designated
  authoritative target`; heroic + makeshift `expected exactly one
  TARGET_SELECTION, found 0` (zones show AI-picked Bear resolution:
  bear-BF + mannequin Effect present, i.e. outcome-coincidence without
  transport); intimidation `production spell cast returned false`.
- Decisive negative evidence: the artifact contains NO
  `decision-requests.jsonl` (not even parent dirs, which the provider
  creates on first request) — the D4c provider saw ZERO
  `ExternalDecisionRequest`s across all 4 cases.

## Root cause (HARNESS / ADAPTER_DECISION_BINDING, pin-evidenced)

Single causal defect: the D4c provider was attached to DETACHED
`PlayerControllerHuman` objects the engine never consults. Chain at Forge
pin `8c7e9afb`:

1. `AITest.initAndCreateGame` registers both players as `LobbyPlayerAi`
   (pin `forge-gui-desktop/.../forge/ai/AITest.java:54-55`).
2. `Game` -> `LobbyPlayerAi.createIngamePlayer` ->
   `ai.setFirstController(aiController)` (pin
   `forge-ai/.../forge/ai/LobbyPlayerAi.java:49-57`); registered
   controllers are AI. `PlayerControllerHuman` constructor does NOT
   self-register (pin `PlayerControllerHuman.java`: constructor only builds
   input proxy/queue).
3. `PlaySpellAbility.playSpellAbility(controller, p, sa)` delegates payment
   and targeting to the REGISTERED controller (`p.getController()`, pin
   `PlaySpellAbility.java:141`; targeting via `SpellAbility.setupTargets`
   -> `targetingPlayer.getController().chooseTargetsFor`, pin
   `SpellAbility.java:2135+`).
4. Therefore all cast-time targeting was answered by the unpatched AI
   controller (invisible: the WS01 boundary hooks Human methods only):
   heroic/makeshift Bear picks by AI heuristic/first-pick coincidence,
   surge non-Shock pick (correctly rejected by the pre-resolution
   designated-target gate), intimidation refused (`AI:RemoveDeck:All`
   posture → cast false).

The harness fail-closed gates worked as designed (no silent credit, no
first-pick laundering, no AI substitution accepted). Semantic target,
fixture semantics, engine targeting semantics, and the TARGET_SELECTION
overlay are all exonerated: the engine never received the provider.

Classification: HARNESS (+ADAPTER_DECISION_BINDING). Not ENGINE, not
FIXTURE, not CERTIFIER (never reached), not INFRASTRUCTURE ( Maven/Forge
build green; overlays applied). Bounded repair authorized: attach the
provider-wired Human controllers as the players' REGISTERED controllers
(`Player.dangerouslySetController`, pin `Player.java:2595+` — the same
field assignment a human-lobby game performs via `setFirstController`,
minus the already-assigned guard). Semantic target, case set, digest,
fixture, and contract unchanged → the single authorized replacement run
may be used after local/static re-validation.

## Retained-evidence finding (no requalification)

The same detached-controller architecture was inherited from D1–D4b
harnesses: their "0 decisions" means zero requests reached the detached
Human provider, while registered AI controllers performed forced acts
(exact-pool mana payment). No demonstrated impact on terminal D4a/D4b
evidence (surveyed decision-free paths; exact life/zone/pool/command-zone
assertions; deterministic replay). Per contract, no D4a/D4b requalification
is opened. Follow-up recorded: future D-family harnesses must use
registered-controller attachment (this repair), and a D4a/D4b prompt audit
(optional-cost/trigger prompts answerable by AI, e.g. blight-payment
decline mechanics) is queued as non-blocking review, never a blocker.

## Exact next action

Apply the bounded harness repair (registered-controller attachment for
actor + opponent), re-run all local gates (py_compile N/A — Java only;
stub-compile; overlay anchors unaffected), commit, push (triggers the
single authorized replacement run), register PENDING, poll, adjudicate.
