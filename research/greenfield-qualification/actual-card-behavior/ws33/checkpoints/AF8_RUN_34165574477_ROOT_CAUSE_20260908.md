# AF8 run 34165574477 — verified root-cause checkpoint

Source/run identity: `6120ca3ee9754cbad4600ee45008ee2455b95465`, tree
`264e881c43c643363f0a499764c0a81ae8285c4e`, job `101875828886`, artifact
`10034108017`, digest
`sha256:5e300282cbfab475d4291b072b1c12c19ac29b64843e93b4d5cc8676b36bcf49`.

The downloaded artifact's outer SHA-256 matched. All 6,186 files were read,
the root and three nested SHA256SUMS manifests verified 6,210 entries with no
issues. The run did not mutate or promote coverage.

The task packet's earlier single unattributed-remote-decision symptom is not
present in this frozen artifact. Its actual fail-closed causes are independent
qualification/runtime-adapter gaps:

1. `Profane Command` reaches `announceRequirements` with a finite optional X
   range, but the legacy GUI integer path rejects external operation before
   the existing exact `chooseNumber` adapter can issue authoritative options.
2. `Aether Tradewinds` reaches target selection and payment, but the generic
   deterministic harness policy hashes the authoritative mana transitions;
   it can spend generic mana and then choose cancel with a coloured component
   remaining. This is a witness policy gap, not a target-legality or Forge
   rules-core failure.
3. The existing client witness uses a secret card in Library. Remote library
   projections need not carry that card identity, so the real client probe has
   no sample to evaluate. A principal-owned hidden Hand card is the correct
   generic fixture for the existing client-projection probe.

The repair retains strict rejection of unattributed gameplay events, never
accepts `CANCEL` as a mana-payment substitute, introduces no card- or path-id
branch, and does not promote coverage. Failure classification remains
`QUALIFICATION_EVIDENCE_CONTRACT_MISMATCH` / P2 adapter-harness gap.
