# ABC-D2 lifelose run 34352333072 — FAIL (adjudicated, root-caused)

Date: 2026-09-09. TERMINAL_RESULT=FAIL. COVERAGE_PROMOTION=FALSE.
COVERAGE_MUTATED=FALSE. No repartition (D_EVIDENCED stays 10).

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34352333072; JOB=102468402626 (`d2-lifelose`, completed failure).
- RUN SOURCE HEAD=`9a1b743ceaacdd8b1df18193ea3447bab233db1c` == run head_sha.
- ARTIFACT=10104260533 (`ws33-abc-d2-lifelose-34352333072`).
- Step 4 (queue/ledger binding) FAILED; steps 5-15 skipped; step 16
  upload PASS. No records, no evidence produced. All 8 D2 paths remain
  UNKNOWN; D1's 10 evidenced paths untouched.

## Root cause

D2_WORKFLOW_CONSTANT_DEFECT (D-local workflow derivation slip): the
binding assertion kept `len(set(ids))==22` from the D1 template while
the D2 item holds 18 unique ids (job log line: `len(set(ids))==22`
AssertionError). Preparer/filter/test/certifier/DIGEST unaffected
(local dry-runs already reproduce `33f4fedf...`).

## Repair

One constant `22`→`18` in the D2 workflow binding step; no other change.

## Exact next action

Push repair (auto-triggers D2 replacement run); register PENDING;
adjudicate.
