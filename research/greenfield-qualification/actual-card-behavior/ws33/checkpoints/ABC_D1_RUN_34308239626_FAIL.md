# ABC-D1 lifegain run 34308239626 — FAIL (adjudicated, root-caused)

Date: 2026-09-09. TERMINAL_RESULT=FAIL. COVERAGE_PROMOTION=FALSE.
COVERAGE_MUTATED=FALSE. No repartition (EVIDENCED stays 0/920).
D-local partition unchanged; all 10 D1 paths remain UNKNOWN.

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34308239626; JOB=102329311808 (`d1-lifegain`, completed failure).
- SOURCE_HEAD=`74e1a8313985624d17b828f83462661c07b2218c` == run head_sha.
- ARTIFACT=10087488046 (`ws33-abc-d1-lifegain-34308239626`, expired=false).
- GitHub ZIP digest `sha256:a55eec86df29f2261660ae6b2c52b317fb95c1908253ba16e10ddf4fbc0f104a`
  reproduced locally (equal). 1186 ZIP entries readable.
- Steps 1-12 PASS (checkout, model artifact, queue/ledger binding with
  TARGET_DIGEST reproduced in-run, pins, all overlays, materialization
  10+12, test installation). Step 13 Record FAILED; 14-15 skipped; 16
  upload PASS (this adjudication's basis).
- `lifegain-record-diagnostics.jsonl`: 10/10 cases failed, 0 markers,
  0 campaign index. Per-case errors: 5x "activated host not on
  battlefield", 1x "ETB host not on battlefield", 2x "resolved spell
  not in graveyard", 2x NPE `paperCard null` in `createCard`.
- Surefire: `Tests run: 1, Failures: 1` (success==0 tripwire), BUILD FAILURE.

## First-causal-failure analysis

Systemic harness fixture/name defect with two coupled parts (both
qualification-harness-side; no Rules-Core, overlay, pin, or
materialization fault):

R1. The preparer derived the TSV card field from the card-file stem
(`zuran orb`, `ayli eternal pilgrim`, `angels mercy`) instead of the
authoritative `Name:` field of the pinned card script (`Zuran Orb`,
`Ayli, Eternal Pilgrim`, `Angel's Mercy`). Forge card lookup and
`getName().equals` comparisons are exact-name-sensitive, so
punctuation-bearing names NPE'd in `createCard` (ayli comma, angels
apostrophe) and all name re-checks failed on case.

R2. The ACTIVATED_* driver never places the witnessed host permanent;
it required fixture pre-placement, which the fixture table provided
only for Trading Post (where R1 still broke the lookup). Zuran Orb,
Pizzasaur, Vampiric Rites, Tainted Sigil, Ayli were never put on the
battlefield.

Encouraging but NON-evidence: avenge/timely/cloudblazer passed their
life-delta asserts before failing on name re-checks (core
cast/trigger/resolve path works). These are not evidence: postcondition
suites, decision/RNG tapes, hidden observation, and replay are all
incomplete for every case. No partial credit granted. UNKNOWN != PASS.

## Root-cause classification

D1_HARNESS_FIXTURE_DEFECT (R1 name mapping + R2 host placement).
Fail-closed behavior held: zero false markers, diagnostics complete,
workflow red. D1a failure witness remains immutable and unreused.

## Exact next action

Repair (test + preparer + workflow TARGET_DIGEST only): exact `Name:`
from pinned scripts; driver-placed ACTIVATED hosts; regenerate +
re-freeze digest; local validation (py syntax, javac parse,
preparer/filter dry-run); push repair; register new PENDING; run D1c.
