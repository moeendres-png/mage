# ABC-D2 lifelose run 34353007000 — FAIL (adjudicated, root-caused)

Date: 2026-09-09. TERMINAL_RESULT=FAIL. COVERAGE_PROMOTION=FALSE.
COVERAGE_MUTATED=FALSE. No repartition (D_EVIDENCED stays 10).

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34353007000; JOB terminal (`d2-lifelose`, completed failure).
- RUN SOURCE HEAD=`a856a934c4feb331407897ae729708bdf766a494` == run head_sha.
- ARTIFACT=10104586883 (`ws33-abc-d2-lifelose-34353007000`).
- Steps 1-10 PASS. Step 11 failed AFTER both in-run gates passed:
  preparer `WS33_ABC_D2_MATERIALIZATION=PASS cases=8 deferred=10
  digest=33f4fedf...` and filter `WS33_ABC_D2_FILTER=PASS cases=8
  deferred=10 digest=33f4fedf...` (job log). The failing line is the
  workflow's own TSV line-count assert `= 10`. Steps 12-15 skipped;
  step 16 upload PASS. No records produced. All 8 D2 paths UNKNOWN.

## Root cause

D2_WORKFLOW_CONSTANT_DEFECT (same derivation-slip family as the
binding slip): `test "$(grep -vc '^#' .../d2-cases.tsv)" = 10` must be
`= 8`. Preparer/filter/test/certifier/digest unaffected.

## Repair

One constant `10`→`8` in the D2 workflow materialization step.

## Exact next action

Push repair (auto-triggers D2 replacement run); register PENDING;
adjudicate.
