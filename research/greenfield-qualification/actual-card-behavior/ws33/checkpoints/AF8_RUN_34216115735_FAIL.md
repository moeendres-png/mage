# AF8 run 34216115735: bounded X diagnostic checkpoint

* Workflow run/job: `34216115735` / `102028131829`
* Workflow source: `77d9a5b97994e30f083ae8fcaadacfae24f0fcdf`
* Outcome: `FAIL`, fail-closed as intended.
* Generic case-summary reporting established the remaining runtime cause in both
  record and replay: the actual mana-X announcement reaches the external
  boundary with an unbounded range.  `chooseNumber` rejected it with
  `UNSUPPORTED_DECISION_PATH: unbounded, oversized, or invalid integer range
  cannot be exported as an exact option set`.
* The compact bundle was built, but its first manifest included its own output
  file, so self-verification correctly failed.  No artifact ID or digest was
  published for this run.
* Next repair: enumerate only the existing Forge payment-solver's test-mode
  feasible mana-X values within a conservative finite source upper bound;
  retain fail-closed behavior for non-mana-X, oversized, or no-feasible-value
  cases.  The bundle manifest excludes itself.
* No coverage registry mutation or promotion occurred.

Classification: `QUALIFICATION_EVIDENCE_CONTRACT_MISMATCH`; no
`CARD_BEHAVIOR_FAILURE` is established.
