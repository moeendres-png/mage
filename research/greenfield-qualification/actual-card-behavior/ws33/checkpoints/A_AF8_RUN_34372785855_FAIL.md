# WS33-A AF8 run 34372785855 — terminal FAIL (2026-09-09)

Status: **FAIL**. `COVERAGE_PROMOTION=FALSE`. `COVERAGE_MUTATED=FALSE`.
Ordering note: this FAIL record is persisted after one A-owned workflow-assertion repair
(`1419a9e213`) that was derived from the run's own step logs; no adjudication-gated source
change was made. The run source `ef836661b3` is immutable and untouched.

## Terminal identity

- SOURCE_HEAD: `ef836661b3c0bf13535f0f417e36367eb6fd540e` (= run `headSha`, verified)
- RUN: `34372785855`; JOB: `102537936108` (`af8`); conclusion `failure`
- ARTIFACT: `10112782348` (`ws33-abc-a-rest-svar-af8-34372785855`, 538468342 bytes, not expired)
- ARTIFACT DIGEST: `sha256:cc8fff47f5742e251e810963d0996e20c16bbd3370d4f81f265e5642f00b7752`
- Sealing/upload steps succeeded (`always()`), so the artifact is complete despite adjudication skip.

## Step-level outcome (DIRECTLY_VERIFIED from job log)

- Steps 1-14 SUCCESS: frozen checkout, parser/hardener/adjudicator validation (38 tests),
  immutable route consumption, pinned deps, Java 21, full overlay stack, harness prep with all
  hardened markers, **Fresh AF8 Record green at JUnit level**.
- Step 15 (`Fresh tape-driven AF8 Replay`) FAILURE: JUnit green (`Tests run: 1, Failures: 0`),
  BUILD SUCCESS, then exit code 1 on a post-run file assertion.
- Step 16 (adjudication) SKIPPED; steps 17-18 (seal/upload) SUCCESS.

## Root-cause classification

1. `WORKFLOW_ASSERTION_DEFECT` (A-owned, repaired): the replay step asserted `test -s` on
   `AF8_SELECTION_WITNESS.tsv`, but Replay serves every recorded decision from tape and never
   re-invokes the pilot, so an empty replay witness is mechanically correct (proven on the local
   replica: identical tapes/events, empty replay witness). The adjudicator already governs this
   (record-side presence + novel-replay-selection rejection). Repaired in `1419a9e213`
   (`test -f` + dropped witness byte-cmp, with rationale comments). No shared semantics touched.
2. `SHARED_FRAMEWORK_BOUNDARY` (open, coordinator decision required): Profane Command
   (`fa2b8db7`) cannot pass on the current shared stack — its X-announcement range is
   `[0, Integer.MAX_VALUE]` and the shared WS01 adapter fail-closes unbounded integer
   externalization before any pilot policy engages (`UNSUPPORTED_DECISION_PATH: GUI_GET_INTEGER`).
   Local replica: 7/8 PASS with full identity/target/selection/stage evidence; Profane FAIL_CLOSED.
   Bounding X is a shared WS01 semantic change (outside A ownership). Artifact adjudication pending
   to confirm the remote 7/8 shape.

## Exact next action

Finish artifact download, verify digest + SOURCE_CHAIN binding to SOURCE_HEAD, run the frozen
adjudicator over remote Record/Replay, then dispatch the replacement run from the repaired tip
and adjudicate it (expect: replay step green, adjudication FAIL_CLOSED on Profane only).

`TURN_STATUS=INTERRUPTED`. `TASK_COMPLETE=NO`.
