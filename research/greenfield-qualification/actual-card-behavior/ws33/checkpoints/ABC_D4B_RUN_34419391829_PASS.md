# ABC-D4b emblem run 34419391829 — PASS (adjudicated)

Date: 2026-09-10. TERMINAL_RESULT=PASS. COVERAGE_PROMOTION=FALSE
(D-local evidencing only). COVERAGE_MUTATED=FALSE.

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34419391829; JOB success (`d4b-emblem` 102691309455).
- RUN SOURCE HEAD=`47bca7ac5fb61255d4165f6a0073abb7cc57e5f1` == run head_sha;
  gate source_head/tree match (`ea2e0d0e…` == tree of HEAD).
- ARTIFACT=10130470897 (`ws33-abc-d4b-emblem-34419391829`).
- GitHub digest `sha256:885ed04b665b088f5fd5070fe0a4e9c99cecec3e6bcf92d8b6729dad218a62bb`.
- Internal `ABC_D4B_HASHES.sha256`: 41 entries, all verify, zero bad.
- Gate `ABC_D4B_GATE.json`: PASS, 3 paths, forge pin `8c7e9afb…`,
  target `0b856b3c…`, 0 decisions (all NONE-intent, pools exact),
  0 RNG, 0 leaks, 0 divergence.
- Diagnostics 0 bytes each; index 3 records.
- Per-path 3/3: markers; EXTERNALLY_RULE_VALIDATED records, all
  assertions PASS (incl `entry-loyalty-8`, `doubling-season-battlefield`,
  `effect-command-present` + `effect-kind-*`), deltas equal; traces bound
  with effect_kind (1 static / 2 trigger) and entry_loyalty 8; tapes
  vacuous (0 events, consistent with NONE intents); observation PASS;
  replay divergence 0.
- Consequence highlights: Elspeth emblem static proven by fixture Wrath of
  God leaving the actor Runeclaw Bear on BF while Elspeth rests in GY via
  engine SBA (loyalty 0); Chandra emblem trigger payload present with host
  at loyalty 1; Tezzeret emblem trigger payload present with host at
  loyalty 2. Entry-doubling (printed 4 → 8) proven on all three production
  casts before ultimate payment.
- Anti-double-credit: 3 ∩ partition-evidenced-33 = 0; ∩ retained-PASS
  `ede58d66` = 0; ∩ non-UNKNOWN ledger = 0.
- Predecessor FAIL 34418252401 (HARNESS_FAILURE certifier
  UnboundLocalError, zero semantic implication) classified before repair;
  its records were never promoted nor reinterpreted.

## Evidenced set (D-local, 3 new)

0006052e (Tezzeret L11, cast-entry + doubling-8 + trigger-presence),
2d5a6d01 (Elspeth L7, cast-entry + doubling-8 + static-consequence),
96a93b32 (Chandra L11, cast-entry + doubling-8 + trigger-presence).

## Standing rules carried (D4a + D4b)

- Planeswalkers enter only via production CAST.
- Multi-turn loyalty buildup is unsupported under the frozen strict
  decision boundary (PRIORITY_ACTION / DECLARE_ATTACKERS rejects vs AI
  discretion) — single-turn setup only.
- Doubling Season entry-doubling is a reusable D-owned setup mechanism
  (Moved-time AddCounter bridge, engine-owned, fail-closed on entry != 8).
- Probe designs must not place duplicate hand copies under a uniqueness
  guard (D4a return-first rule, retained).
- Pin-derived API stub compile is a required local gate for harness
  changes (D4b API surface ⊆ D4a, recompiled clean).

## D partition after repartition

EVIDENCED=36, REMAINING_UNKNOWN=884 (incl. 1 template-missing blocked).

## Exact next action

D4c target/choice-gated batch per the D4 shape survey (21 paths, D6 family):
per-path survey first, then bounded machinery on an isolated follow-up.
Do not broaden beyond the surveyed D4c set. D4b is terminal.
