# WS33 G3 non-AF Principal Observation — tooling boundary

STATUS = DIAGNOSED
EVIDENCE_CLASS = CODE_DERIVED

Pre-write branch lineage already contains frozen G3.4 PASS checkpoint and handoff.

Direct source findings:

1. Existing `ws33_instrument_g_principal_observations.py` supports exactly two attribution anchors: `DIRECT_G` and `G_SVAR_AF`. It fails closed for missing/mixed anchors and therefore cannot currently instrument the non-AF `G_SVAR_EVENT` harness.
2. The non-AF runtime harness generated by `ws33_prepare_g_svar_event_harness.py` has a distinct production-reachable parent-event campaign anchor: after `awaitRemoteTransport(ps)` it sets `currentPath.set(spec.pathId); ws33CurrentParentKey.set(pk);` and then dispatches the source event through TriggerHandler/stack reachability. This is the correct place to bind observation path attribution; target SVar is not entered directly.
3. The existing principal-observation v5 adjudicator is generic except for shape-aware hidden-consumer classification and delegates to the common/v4 lifecycle contract. It can be reused after the event harness emits path-scoped `PRINCIPAL_OBSERVATIONS.jsonl`.
4. The non-AF event case ABI already marks hidden-required paths in field 15: 31 unique hidden-required effective paths out of 32. G3.4 independently verified this obligation count.
5. G3.5 must bind exact frozen runtime `9957712911` and G3.4 certification `9957878386`, retain the same Forge/model/topology lineage, and must not mutate coverage.

Systemic tooling repair required:
- extend qualification-only observation instrumentation with one explicit `G_SVAR_EVENT` attribution anchor matching the event harness shape;
- clear path in the event harness finally block;
- export payload-free observation trace exactly as existing Direct/AF campaigns do;
- create a non-AF principal-observation workflow that re-executes the 33 source-parent campaign under observation instrumentation for the 31 hidden-required paths, while binding the frozen runtime/G3.4 artifacts as compatibility prerequisites;
- adjudicate principal-scoped grant/visible/revoke/hidden lifetime, actor/principal stability, Record/Replay observation equivalence, zero cross-principal leaks and zero hidden-card-ID leakage;
- no card-name/path-ID branching, no Rules mutation, no pilot fallback, no coverage promotion.

NEXT_ACTION = make exactly one tooling/workflow commit implementing this generic event-attribution support and G3.5 workflow; verify exact-source run cardinality, persist PENDING immediately, then WRITE_FREEZE.
