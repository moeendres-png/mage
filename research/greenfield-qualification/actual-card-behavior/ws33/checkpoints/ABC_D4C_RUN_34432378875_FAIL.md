# ABC-D4c single-target FINAL run 34432378875 — FAIL (terminal, adjudicated, FAIL CLOSED)

Date: 2026-09-10. Branch: `work/ws33-d-high-throughput-20260907`.
TERMINAL_RESULT=FAIL. COVERAGE_PROMOTION=FALSE. No coverage change:
D-local evidence stays 36 / 884. All 4 T1 paths remain UNKNOWN. This was
the single FINAL authorized D4c-T1 execution. No fourth run is authorized.
D4c terminates FAIL CLOSED. No further repair-run cycle.

## Independent adjudication (DIRECTLY_VERIFIED)

- RUN=34432378875 (`WS33 ABC D4c target closure`), JOB `d4c-target`
  102730394507, conclusion failure, 4m3s. RUN SOURCE HEAD=
  `eb6235f413cba3587f223c86a13260660a78e011` == run head_sha == frozen
  Repair #2 commit; no source drift. SOURCE_TREE=
  `1ff41cb01262524f8a97d9a0085b02a35a0a137e`.
- Boundary steps all PASS (checkout, model, queue/ledger 49, Forge/WS01/
  WS12/WS32 pins, WS01/WS12/WS32/WS05/WS06/target-selection overlays,
  materialization 4+45 digest `1c16bf25…`, harness install).
  `Execute ABC-D4c Record` FAIL: 0/4; diagnostics non-empty (4 entries).
  Replay and Certify never reached (correct sequencing, no silent credit).
- ARTIFACT=10135026999 (`ws33-abc-d4c-target-34432378875`), digest
  `sha256:c8b7f7e68b7fbde7f4cde10317d29d3a24c7757b1d4eb7db6b320f6fb4030089`
  (verified: local zip sha256 matches API digest; extracted under
  `/tmp/d4c-final-34432378875/x`).
- Per-case diagnostics (artifact): all 4
  `ExternalDecisionValidationException UNSUPPORTED_DECISION_PATH:
  external decision provider failed` (engine-side wrap of the provider's
  fail-closed throw; no silent credit). Same wrap as run 34429297453,
  different throwing decision (see root cause).
- Decisive structural evidence (artifact `campaign/d4c/records/*/decision-
  requests.jsonl`): every case logged exactly 2 requests — line 1
  TARGET_SELECTION (exactly 2 entity-backed CARD options,
  `option_id=card:<id>`, `entity_kind=CARD`, `semantic_value=<id>`,
  `min=max=1`, `cancel_allowed=true`), line 2 MANA_PAYMENT (discrete
  POOL:N options + CANCEL, `cancel_allowed=false`; option counts 7/7/5/3
  across surge/heroic/makeshift/intimidation). Zero `record-success.marker`
  files. Surefire terminal: `d4cTargetCampaign:114 ... produced no
  successful record cases`.

## Repair #2 verdict (bounded, log-evidenced)

The entity-id unique-match correction WORKED in its bounded scope: on all
4 cases the provider answered TARGET_SELECTION without a unique-match
throw (zero `matches!=1` failures; the engine accepted each well-formed
target response and advanced the cast past targeting to mana payment —
the second request line exists on all 4, which is impossible if the first
decision had thrown). The `CARD:<id>` encoding defect of run 34429297453
is cured. End-to-end target proof was NOT achieved: the pre-resolution
designated-target gate, tape-driven replay, and certifier were never
reached, so no per-path certification exists and none is claimed.

## Root cause (HARNESS, decision-coverage gap exposed by Repair #1)

The provider handles only TARGET_SELECTION in record mode and throws
fail-closed `unexpected decision kind` on anything else. With the
registered Human controller (Repair #1, run 34429297453), the engine now
routes mana payment through the external-decision surface as MANA_PAYMENT
(previously the detached architecture let registered AI controllers pay
invisibly via exact-pool forced acts, so no MANA_PAYMENT ever surfaced —
the T1 survey assumption of "exactly one native callback" held only for
the detached configuration and is empirically falsified for the
registered-controller configuration). All 4 casts therefore abort at mana
payment, before resolution. Engine targeting, fixture pools, overlay
transport, and case design are exonerated: the engine offered genuine
2-candidate target discretion, accepted the target responses, and
correctly wrapped the fail-closed throw.

Classification: HARNESS (+ADAPTER_DECISION_BINDING lineage: Repair #1
exposed the mana-payment surface; Repair #2 covered targeting but not
payment). Not ENGINE, not FIXTURE (pool/case design unchanged; the mana
prompt is a controller-surface consequence, not a pool error), not
CERTIFIER (never reached), not INFRASTRUCTURE (Maven/Forge build green;
all overlays applied; pins verified). A complete harness would need a
MANA_PAYMENT branch co-designed with fixture/contract — explicitly OUT
OF SCOPE: no fourth run is authorized, so no such repair is attempted.

## Terminal state

- D4c-T1: 0/4 certified. All four T1 paths stay UNKNOWN. D-local evidence
  unchanged at 36 evidenced / 884 remaining. Predecessor D4a/D4b evidence
  untouched. No coverage promotion, no reinterpretation of prior records.
- D4c workstream: TERMINATED FAIL CLOSED. No further D4c runs. Do not
  start D4d/D4e/D4f. Do not start Batch-6. Do not run Full107.
- ARCHITECTURE_FREEZE = NOT CLAIMED. PRODUCTION_PROVIDER = NOT SELECTED.

## Exact next action

None in D4c (terminated). Any future single-target work requires fresh
coordinator authority on a new workstream with a MANA_PAYMENT-inclusive
provider/fixture/contract design and its own frozen-source registration;
it must not reuse, reinterpret, or mutate this run's artifact
(10135026999, digest above, immutable).
