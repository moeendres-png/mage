# WS33-A AF8 run 34403594655 — terminal STEP-FAILURE with semantic PASS (2026-09-09, reconciled)

Status: **FAIL_CLOSED_STEP** (workflow step 16 red; semantic gate PASS 8/8).
`COVERAGE_PROMOTION=FALSE`. `COVERAGE_MUTATED=FALSE`.
Ordering note: this record is persisted before the workflow-assertion repair commit
derived from this run's own step log; the run source `b2289eb6e8` is immutable and
untouched. No adjudication-gated source change was made to the run source.

## Terminal identity (DIRECTLY_VERIFIED from live GitHub API + job log)

- SOURCE_HEAD: `b2289eb6e8ebe38b7a5e2f2996d64e125e0d0708` (= run `headSha`, verified)
- SOURCE_TREE: `6634ff3f2cf16fddf25f986b373f1ecbc1a99d5e`
- RUN: `34403594655`; JOB: `102641128995` (`af8`); conclusion `failure`
- ARTIFACT: `10124574899` (`ws33-abc-a-rest-svar-af8-34403594655`, not expired)
- ARTIFACT DIGEST (API): `sha256:2d54c3d880db22444d0a65e626e1fb8a0c31baccea20fa00322840b6c7063ad7`
  (byte recomputation + SOURCE_CHAIN binding verification still open: artifact
  download stalled on size; tracked as verification debt, not a verdict input)
- Sealing/upload steps succeeded (`always()`), so the artifact is complete.

## Step-level outcome (DIRECTLY_VERIFIED from job log)

- Steps 1-13 SUCCESS: frozen checkout, validation (41 local tests green in CI),
  immutable route consumption, pinned deps, Java 21, full overlay stack **including
  `WS33_BOUNDED_NUMBER_EXTERNALIZED=PASS ... x_mana_feasibility=true`**, harness prep
  with all hardened markers incl. new `POSITIVE_X_KIND=GUI_GET_INTEGER_NUMBER_NUMBER_ENUM`.
- Step 14 (Record) SUCCESS, step 15 (Replay) SUCCESS at JUnit level.
- Step 16 (adjudication) FAILURE on a single shell assertion **after** the frozen
  adjudicator itself reported `WS33_A_SVAR_AF8_GATE=PASS paths=8` with
  `failure_count=0, failures=[]` over `expected_paths=8`.
- Failing assertion: `cmp generated/record/AF8_CLIENT_OBSERVATION_SAMPLES.tsv
  generated/replay/AF8_CLIENT_OBSERVATION_SAMPLES.tsv differ: byte 196, line 3`.

## Root-cause classification

1. `WORKFLOW_ASSERTION_DEFECT` (A-owned, repair pending, mirrors qualified-line
   `1922a5172f` Case A): aggregate async remote-observation sample multiplicity is
   transport, not semantics. The adjudicator's semantic contract (>=1 real remote
   sample per principal, zero visibility mismatch) is PASS on both sides for all
   eight paths; the workflow's stale byte-equality `cmp` on the samples file is the
   defect. Same defect class as the `1419a9e213` replay-witness assertion precedent.
2. Coordinator question answered at semantic level (CODE_DERIVED, artifact
   confirmation open): gate PASS 8/8 with the strict X-presence rule satisfied means
   Profane Command's unbounded mana-X domain was source-authoritatively enumerated
   by the reconciled shared mechanism — the overlay intercepts `max == MAX_VALUE`
   before the GUI adapter, so the X row is necessarily `NUMBER_ENUM` (Forge
   payability enumeration), selected smallest-positive by the pilot among exact
   options. No new WS01 contract was needed; no escalation triggered.

## Exact next action

Remove the stale samples `cmp` (port of the qualified line's accepted repair) with
its contract regression test; keep every semantic gate and `cmp` unchanged; commit;
dispatch one replacement run from the repaired tip; adjudicate its artifact fully
(digest recompute + SOURCE_CHAIN + gate + per-path evidence). No coverage promotion.

`TURN_STATUS=INTERRUPTED`. `TASK_COMPLETE=NO`.
